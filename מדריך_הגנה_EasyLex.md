# 🎯 מדריך הגנה — EasyLex
### זאב פריימן | 18/6 שעה 14:00 | חצי שעה

---

> **איך הוא שואל:** הוא קרא את הספר שלך לפני הבחינה ומגיע מוכן.
> הוא לא שואל "מה זה Room" — הוא שואל **"למה Room? מה עשית אחרת?"**
> **הנוסחה לכל תשובה:** מה בחרתי ← למה ← מה החלופה ← מה היתרון של הבחירה שלי

---

# 🟢 חלק 1 — שאלות קלות (התחל מכאן)

> אלה השאלות שהוא יתחיל איתן. תדע אותן בעל-פה. הן פשוטות כי זה **הפרויקט שלך**.

---

## "ספר לי על הפרויקט שלך — מה הוא עושה?"

**אמר:**
> EasyLex היא אפליקציית Android שעוזרת לתלמידי תיכון ללמוד מילים באנגלית.
> הרעיון: תלמיד רואה מילה בספר, מצלם אותה עם הטלפון, מקבל תרגום עברי מיידי עם הסבר,
> והמילה נשמרת אוטומטית לתרגול עתידי.
> יש חמישה מודולי תרגול: שאלון, כרטיסיות, אלוף האיות, תיקון טעויות ולפי נושאים.
> כל תרגול מעלה XP ורמה — כמו משחק.

---

## "מה הבעיה שרצית לפתור?"

**אמר:**
> תלמידים פוגשים מילים חדשות בכל שיעור אנגלית, אבל אין להם כלי נוח לתעד ולחזור עליהן.
> מחברת — שוכחים לפתוח אותה. Google Translate — מתרגם אבל לא מלמד, אין חזרה.
> EasyLex פותרת את שלושת השלבים ביחד: צלם → תרגם → תרגל.

---

## "כמה מסכים יש? תסביר כל אחד בקצרה"

| מסך | מה הוא עושה |
|-----|------------|
| **Splash** | בדיקה אם המשתמש מחובר — עובר ישירות לבית, אחרת להתחברות |
| **Login / Register** | הרשמה והתחברות עם Firebase Auth |
| **תרגול (PracticeFragment)** | 6 כרטיסים עם Badge — שאלון, כרטיסיות, איות, טעויות, נושאים, אישי |
| **שאלון (QuizFragment)** | שאלה עברית + 4 תשובות אנגליות, XP על נכונה |
| **אלוף האיות (SpellingFragment)** | רואה תרגום, שומע מילה, כותב אותה נכון |
| **כרטיסיות (FlashcardsFragment)** | כרטיס הפיך — לחיצה = סיבוב, החלקה = ידעתי/לא ידעתי |
| **סריקה (ScanFragment)** | מצלמה → OCR → Gemini → שמירה |
| **המילים שלי (MyWordsFragment)** | רשימת כל המילים עם חיפוש וסינון |
| **סטטיסטיקות (StatisticsFragment)** | XP, רמה, Streak, אחוז שליטה |
| **הגדרות (SettingsFragment)** | שם משתמש, יעד יומי, TTS, ניקוי נתונים |

---

## "תסביר לי את מסך התרגול — ה-6 כרטיסים"

**אמר:**
> מסך התרגול הוא הבית של האפליקציה. יש בו רשת של 6 כרטיסים.
> כל כרטיס מציג Badge — מספר שמראה מה המצב:
> - **מבחן יומי:** X/20 — כמה מתוך 20 השאלות היומיות הצלחתי
> - **תיקון טעויות:** כמה מילים עשיתי עליהן טעות לאחרונה
> - **אלוף האיות:** כמה מילים כתבתי נכון מתוך כלל המילים
> - **כרטיסיות:** כמה מילים "נוצחו" (ידעתי ← לא טעיתי)
> - **המילים שלי:** כמה מילים הוספתי אישית
> בראש המסך יש שורת XP — רמה, סרגל התקדמות, ורצף יומי (Streak).

---

## "יש כמה דרכים להציג הודעה. מה בחרת ולמה?"

זו השאלה המפורסמת של זאב פריימן. תדע אותה בע"פ.

**שלוש האפשרויות:**

| שם | מה זה | מתי השתמשתי |
|----|-------|-------------|
| **Toast** | הודעה קטנה שנעלמת לבד, בלי לחיצה | פעולות קטנות שהצליחו: "המילה נשמרה", "הסנכרון הצליח" |
| **Snackbar** | כמו Toast אבל עם כפתור פעולה | לא השתמשתי — יכול היה להתאים ל"בטל" |
| **AlertDialog** | חלון שחוסם הכל ומחכה לאישור | פעולות שלא ניתן לבטל: מחיקת מילים, איפוס XP |

**מה אמר:**
> "בחרתי Toast לדברים פשוטים כי הוא לא מפריע למשתמש — הוא פשוט מציג ונעלם.
> כשמשתמש מוחק את כל המילים האישיות שלו — זה לא הפיך. שם השתמשתי ב-AlertDialog כי
> רציתי שהמשתמש יאשר במפורש לפני שמשהו נמחק לתמיד."

---

## "מה זה Firebase Auth ולמה השתמשת בו?"

**אמר:**
> Firebase Auth הוא שירות של Google שמנהל הרשמה והתחברות.
> הוא שומר סיסמאות בצורה מוצפנת, ומספק לי פונקציות מוכנות: הרשמה, התחברות, שכחתי סיסמה.
> **בלי Firebase Auth** הייתי צריך לבנות שרת שמנהל משתמשים, להצפין סיסמאות בעצמי —
> עבודה של חודשיים. Firebase נתן לי את זה ב-10 שורות קוד.

---

## "מה זה SharedPreferences ומתי השתמשת בו?"

**אמר:**
> SharedPreferences הוא כמו "פתק שאנדרואיד שומר בשבילי" — מפתח ← ערך.
> השתמשתי בו לשלושה דברים:
> 1. **הגדרות** — יעד יומי (כמה מילים), האם TTS פועל, מצב סריקה
> 2. **זמן הסנכרון האחרון** — כדי לא לסנכרן יותר מפעם ב-24 שעות
> 3. **ציון מבחן יומי** — הציון שהשאלון מעביר למסך התרגול
>
> **למה לא Room?** Room מתאים לנתונים מורכבים עם הרבה שדות.
> לערך אחד כמו "האם TTS פועל (true/false)" — SharedPreferences מספיק ופשוט יותר.

---

## "מה זה Mastery Level של מילה? איך מחשבים?"

**אמר:**
> Mastery Level הוא מספר בין 0 ל-5 שמראה כמה טוב אני מכיר מילה.
> 0 = לא מוכר כלל. 5 = שולט לגמרי.
>
> הנוסחה: `min(5, floor(תשובות נכונות / (סך ניסיונות + 1) × 5))`
>
> דוגמה פשוטה:
> - ניסיתי פעם אחת ועניתי נכון → Mastery 2
> - ניסיתי 5 פעמים ועניתי נכון 5 פעמים → Mastery 4
> - ניסיתי 5 פעמים ועניתי נכון 2 פעמים → Mastery 1 (ירד)
>
> ה-`+1` במכנה מונע חלוקה ב-0 ומקשה קצת להגיע ל-5 — צריך להוכיח שליטה אמיתית.

---

## "מה זה XP ואיך הרצף היומי עובד?"

**אמר:**
> XP = Experience Points, נקודות ניסיון כמו במשחק.
> - **+10 XP** על כל תשובה נכונה בכל מודול
> - **+100 XP בונוס** כשמילה עוברת לרמת שליטה 5 (Mastery Level 4 → 5)
> - **+50 XP** על סיום מודול שלם
>
> **רמה:** `floor(√(XP÷100)) + 1` — כל רמה דורשת יותר XP מהקודמת.
>
> **Streak (רצף יומי):**
> - אם פתחתי ולמדתי אתמול ← היום: Streak+1
> - דילגתי יום: Streak מתאפס ל-1
> - אותו יום כמה פעמים: לא משתנה

---

## "מה ה-isFavorite וה-isVerified?"

**אמר:**
> כל מילה במסד הנתונים מסומנת עם שני דגלים שמגדירים מאיפה היא הגיעה:
>
> `isVerified = true` → מילה מהמאגר הגלובלי של Firestore (1,902 המילים הקבועות)
> `isFavorite = true` → מילה שהמשתמש הוסיף בעצמו (סרק עם OCR)
>
> **למה שניהם?** כדי שאוכל לסנן: "הצג רק המילים שלי" = `WHERE isFavorite = 1`.
> וכשאני מסנכרן מ-Firestore — אני יודע לא להחליף את המילים שהמשתמש הוסיף.

---

## "למה השתמשת ב-RecyclerView ולא ב-ListView?"

**אמר:**
> ListView הוא ישן יותר ופחות גמיש. RecyclerView הוא ה-View המומלץ כיום של אנדרואיד לרשימות.
> **היתרונות של RecyclerView:**
> - משחזר (Recycles) Views שיצאו מהמסך — חסכוני בזיכרון
> - תומך ב-GridLayoutManager (רשת עמודות) — השתמשתי בו למסך התרגול עם 2 עמודות
> - תומך בעיצובים מורכבים יותר
>
> **ViewHolder Pattern:** RecyclerView עובד עם ViewHolder — כל פריט ברשימה
> מחזיק refs לViews שלו. בלי זה, כל גלילה הייתה מחפשת Views מחדש — איטי.

---

## "מה ה-Badge בכל כרטיס? מאיפה הנתונים?"

**אמר:**
> כל Badge מחושב אחרת:
> - **מבחן יומי (X/20):** קורא מ-SharedPreferences. כשהשאלון מסתיים — שומר ציון + תאריך.
>   כשמסך התרגול נפתח — קורא: אם התאריך הוא היום → מציג הציון, אחרת → 0/20.
> - **תיקון טעויות:** סופר מילים שיש להן `errorInQuiz=true` או `errorInFlashcards=true`
> - **אלוף האיות:** סופר מילים שיש להן `spellingCorrect > 0`
> - **כרטיסיות:** מילים שנראו ולא שגויות אחרונה
> - **המילים שלי:** סופר `WHERE isFavorite=1`

---

## "מה השתמשת ב-CameraX? מה זה?"

**אמר:**
> CameraX הוא ספריית מצלמה של Android Jetpack — שכבת הפשטה מעל ה-Camera2 API.
> Camera2 מורכב מאוד — עשרות שורות קוד לצילום פשוט.
> CameraX מפשט את זה ל-10 שורות.
> הוא גם מנהל בעצמו הבדלים בין מכשירים — אין צורך לכתוב קוד שונה לכל יצרן.

---

## "מה OCR? תסביר את הזרימה"

**אמר:**
> OCR = Optical Character Recognition = הטלפון "קורא" טקסט מתמונה.
>
> הזרימה ב-EasyLex:
> 1. המשתמש לוחץ "צלם" → CameraX מצלם תמונה
> 2. רואים תצוגה מקדימה ואפשר לאשר או לצלם שוב
> 3. לאחר אישור — ML Kit מנתח את התמונה ומחזיר את הטקסט
> 4. הטקסט נשלח ל-Gemini AI שמחזיר תרגום + חלק דיבר + משפט לדוגמה
> 5. המילה נשמרת ב-Room ועוברת ל"המילים שלי" לתרגול

---

## "מה Gemini API?"

**אמר:**
> Gemini הוא מודל בינה מלאכותית של Google — כמו ChatGPT אבל של Google.
> שלחתי לו את המילה שה-OCR זיהה, עם הנחיה:
> "תחזיר לי JSON עם תרגום עברי, חלק דיבר, משפט לדוגמה וקטגוריה".
> הוא מחזיר JSON מסודר שאני מפרק ובונה ממנו את אובייקט המילה.
>
> **למה לא ML Kit Translate?** ML Kit Translate רק מתרגם. Gemini נותן גם
> חלק דיבר, משפט לדוגמה, וקטגוריה — הכל בקריאה אחת.

---

## "מה Firebase Firestore ואיך השתמשת?"

**אמר:**
> Firestore הוא מסד נתונים בענן של Google — NoSQL (לא טבלאות, אלא מסמכים).
> השתמשתי בו לשני דברים:
> 1. **מאגר המילים הגלובלי** — 1,902 מילים מאורגנות תחת `word_lists/band_2_full_list/words`
> 2. **נתוני Gamification** — XP, רמה, Streak לכל משתמש תחת `users/{uid}`
>
> **למה Firestore ולא שרת רגיל?** Firebase נותן לי את כל התשתית מוכנה.
> לבנות שרת בעצמי היה לוקח חודשים — Firebase לקח שבוע.

---

## "מה ה-ViewBinding?"

**אמר:**
> ViewBinding הוא מנגנון שאנדרואיד יוצר בשבילי קלאס Java מכל קובץ XML.
> במקום לכתוב `(Button) findViewById(R.id.btnStart)` — כותב פשוט `binding.btnStart`.
>
> **יתרון:** אם כתבתי ID שגוי — שגיאה בזמן קומפילציה, לא בזמן ריצה.
> **חשוב:** `binding = null` ב-`onDestroyView` כדי שלא יהיה Memory Leak.

---

# 🟡 חלק 2 — שאלות בינוניות

> אלה שאלות שדורשות קצת יותר הכנה — תדע לענות בשתי-שלוש משפטים.

---

## "למה Room? מה החלופה?"

**אמר:**
> Room הוא ספריה שעוטפת SQLite ומפשטת אותו.
> **חלופה: SQLite גולמי** — הייתי צריך לכתוב Cursor, ניהול עמודות ידני, הרבה קוד.
> Room נותן לי: @Query שנבדק בקומפילציה, LiveData מובנה, type-safety.
> **דוגמה:** `@Query("SELECT * FROM words_table WHERE isFavorite = 1")`
> Room מייצר את כל קוד ה-Cursor אוטומטית — אני כותב רק את ה-SQL.

---

## "למה MVVM?"

**אמר:**
> MVVM = Model-View-ViewModel. מפריד את האפליקציה לשלוש שכבות:
> - **Fragment (View):** רק מציג נתונים. לא מכיר את ה-DB.
> - **ViewModel:** מחזיק את הלוגיקה. **שורד סיבוב מסך** — זה החשוב!
> - **Repository:** מקור אמת אחד — מחליט מה מגיע מ-Room ומה מ-Firestore.
>
> **בלי MVVM:** אם הייתי שם הכל ב-Fragment — סיבוב מסך היה מאפס את השאלון.
> כל הנתונים אובדים. עם ViewModel — הנתונים נשמרים.

---

## "מה זה LiveData?"

**אמר:**
> LiveData הוא מחזיק נתונים שמודע למסך — כשה-DB משתנה, ה-UI מתעדכן לבד.
> לא צריך לרשום ולמחוק Listeners ידנית — LiveData עושה את זה אוטומטי.
> כשFragment נהרס — הSubscription מתבטלת מאפסהּ. אין Memory Leak.
> Room מחזיר LiveData מהקופסה — שמתי Observer פעם אחת ואני מקבל עדכונים תמיד.

---

## "למה Java ולא Kotlin?"

**אמר:**
> Kotlin היא השפה המועדפת של Google מ-2019, אבל **כל ספריות Jetpack עובדות ב-Java**.
> בחרתי Java כי זה מה שלמדתי לעומק — פיתוח בשפה שאני מכיר טוב מפחית באגים.
> Kotlin הייתה מחייבת ללמוד שפה חדשה בזמן שבנוסף בניתי פרויקט מורכב.
> **בדיעבד:** Kotlin היתה חוסכת קוד (Data Classes, Null Safety), אבל Java עשתה את העבודה.

---

## "מה זה Fragment לעומת Activity?"

**אמר:**
> Activity היא "חלון" שלם — יש לה מחזור חיים משלה, מנוהלת ע"י מערכת Android.
> Fragment הוא "תת-מסך" שחי בתוך Activity — קל יותר לעבור בין Fragments.
>
> **מה הפרויקט עושה:**
> - **Activities:** SplashActivity, LoginActivity, RegisterActivity, MainActivity — אלה כי הן "שערי כניסה" נפרדים
> - **Fragments:** כל שאר המסכים — כי Navigation Component עובד איתם, המעבר מהיר יותר, אפשר לשתף ViewModel

---

## "מה השתמשת ב-Navigation Component?"

**אמר:**
> Navigation Component הוא ספריית Jetpack שמנהלת ניווט בין Fragments.
> יש לי קובץ `nav_graph.xml` שמגדיר ויזואלית את כל המסכים והמעברים.
> **החלופה:** FragmentManager.beginTransaction() — ידני וסבוך. צריך לנהל Back Stack לבד.
> Navigation Component מנהל Back Stack אוטומטית — לחיצת "חזרה" פשוט עובדת.
> העברת נתונים בין מסכים (כמו סוג השאלון) — Bundle פשוט.

---

## "מה זה ExecutorService? למה צריך?"

**אמר:**
> אנדרואיד אוסר לגעת ב-DB ב-Main Thread — הוא Thread שמציג את ה-UI.
> אם אפשר פעולת DB שם — ה-UI נתקע.
> ExecutorService יוצר Thread Pool — קבוצת Threads ברקע שמטפלים ב-DB.
> יצרתי Pool של 4 Threads: `Executors.newFixedThreadPool(4)`.
> כל insert/update/delete רץ ב-Background Thread.
> התוצאה חוזרת ל-UI Thread דרך LiveData.

---

## "מה הבאג הגדול ביותר שהיה לך ואיך פתרת?"

**אמר:**
> הבאג הכי גדול: כל נתוני התרגול נמחקו בכל פעם שניווטתי בין מסכים.
> הBadges הציגו 0/1902 ו-0/20 — כאילו לא תרגלתי כלום.
>
> **מה גרם לזה:** הסנכרון עם Firestore הכניס מילים חדשות אבל **קודם מחק את כל הישנות** —
> ואיתן כל נתוני התרגול (correctAttempts, totalAttempts). זה קרה בכל פעם שנוצר ViewModel חדש.
>
> **הפתרון:** שניתי את הסנכרון — **רק מוסיף מילים חדשות**, לעולם לא מוחק.
> בניתי רשימה של מילים קיימות, ורק מילים שלא ברשימה — נכנסות. הישנות נשמרות עם הנתונים שלהן.
> בנוסף: הסנכרון רץ לכל היותר **פעם ב-24 שעות** — לא בכל ניווט.

---

## "למה TTS? מה זה? איך עובד?"

**אמר:**
> TTS = Text-To-Speech — מנוע קריאה מובנה של אנדרואיד שקורא טקסט בקול.
> השתמשתי בו ב"אלוף האיות": המשתמש שומע את המילה ואז צריך לכתוב אותה.
> זה עוזר ללמוד גם את ההגייה הנכונה, לא רק את הכתיב.
>
> **חשוב: `onDestroyView`** — כשיוצאים מהמסך, חייב לכבות את TTS (`tts.stop()` + `tts.shutdown()`).
> בלי זה — הקול ממשיך לנגן ב-Background. היה לנו בדיוק הבאג הזה בניסוי המשתמשים.

---

## "מה ניסוי המשתמשים? מה גילית?"

**אמר:**
> שלחתי APK ל-9 בודקים: אחות, בן דוד, ו-7 תלמידי תיכון + מורה לאנגלית.
> הם השתמשו שבוע ושלחו משוב.
>
> **מצאנו 10 באגים.** הכי חשובים:
> - **הסנכרון מחק נתונים** (תוקן — Insert-Only)
> - **OCR הפוך** כשמחזיקים טלפון בצד שמאל (תוקן — סיבוב Bitmap)
> - **Badge "0/20"** גם אחרי מבחן (תוקן — SharedPreferences)
> - **אין כפתור חזרה** במסכי תרגול (תוקן — הוספת Toolbar)
>
> **כל 10 הבאגים תוקנו** לפני ההגשה.

---

# 🔴 חלק 3 — שאלות מורכבות (אם הוא מעמיק)

> אלה שאלות שדורשות הסבר טכני. לא חייב לדעת בע"פ — הבן את הרעיון.

---

## "מה זה Singleton? למה השתמשת?"

**אמר:**
> Singleton = דפוס עיצוב שמבטיח שיש רק **מופע אחד** של קלאס בכל האפליקציה.
> השתמשתי ב-GamificationEngine — כי אסור שיהיו שני מנועי XP.
> אחד יחשב 1,250 XP ואחר 980 XP — מי צודק?
>
> **הבעיה הטכנית:** מה אם שני Threads יוצרים שני מופעים ברגע זהה?
> פתרתי עם `synchronized` (רק Thread אחד יוצר) ועם `volatile` (הThread לא יחזיק גרסה ישנה בCache).

---

## "מה זה `volatile`?"

**אמר:**
> כשThread מריץ קוד, הוא לפעמים שומר ערכים ב-Cache המקומי שלו — מהיר יותר.
> הבעיה: Thread A שינה ערך, אבל Thread B רואה את הגרסה הישנה מה-Cache שלו.
> `volatile` מחייב כל Thread לקרוא ישירות מהזיכרון הראשי — תמיד גרסה עדכנית.
> בSingleton: חיוני שכל Thread יראה האם ה-INSTANCE כבר נוצר — בלי `volatile` ייתכן שלא.

---

## "למה HashSet לסנכרון?"

**אמר:**
> כשאני מסנכרן מ-Firestore, צריך לבדוק לכל מילה: האם היא כבר קיימת ב-Room?
> אם אשמור את הקיימות ב-List — כל בדיקה עוברת על כל הרשימה. עם 1,902 מילים — איטי.
> HashSet מאפשר בדיקה במיידית — לא משנה כמה מילים יש.
> מעשית: הסנכרון הופך ממספר שניות לפחות משנייה.

---

## "מה זה Memory Leak? איך מנעת?"

**אמר:**
> Memory Leak = Fragment/Activity נהרסים אבל לא נאספים על ידי Garbage Collector
> כי משהו עוד מחזיק בהם.
>
> **איך מנעתי:**
> 1. `binding = null` ב-`onDestroyView` — ViewBinding לא מחזיק ב-Views שנהרסו
> 2. `tts.shutdown()` ב-`onDestroyView` — TTS לא מחזיק ב-Context
> 3. `handler.removeCallbacksAndMessages(null)` — ביטול Callbacks ממתינים
> 4. LiveData עם `getViewLifecycleOwner()` — Observer מתבטל כשFragment נהרס

---

## "מה `getViewLifecycleOwner()` לעומת `this` בObserver?"

**אמר:**
> כשFragment ב-Back Stack — הוא חי אבל ה-View שלו נהרס.
> `observe(this, ...)` → Observer חי כל עוד Fragment חי (גם ב-Back Stack) — ייתכן crash
> `observe(getViewLifecycleOwner(), ...)` → Observer חי רק כל עוד ה-View קיים — בטוח יותר
> תמיד השתמשתי ב-`getViewLifecycleOwner()` ב-onCreateView.

---

## "מה `fallbackToDestructiveMigration()`?"

**אמר:**
> כשמשדרגים גרסת Room (למשל מ-2 ל-3) — Room לא יודע איך לשנות את הטבלה הישנה.
> צריך לכתוב Migration — קוד שמעדכן את המבנה.
> `fallbackToDestructiveMigration()` אומר: "אם אין Migration — מחק הכל ובנה מחדש".
> לפרויקט פיתוח זה נוח — לא צריך לכתוב Migration לכל שינוי.
> **חיסרון:** המשתמש מאבד את נתוני ה-DB המקומי בשדרוג. Firestore עדיין שומר הכל.

---

## "מה ההבדל בין `.set()` ל-`.set(merge())` ל-`.update()` ב-Firestore?"

**אמר:**
> שלוש דרכות לכתוב מסמך ב-Firestore:
> - `.set(data)` — מחליף **הכל**. אם שלחתי רק `displayName`, כל שאר השדות (XP, רמה) — נמחקים.
> - `.set(data, SetOptions.merge())` — כותב רק את מה ששלחתי, שומר את השאר. זה מה שהשתמשתי בו לעדכון שם.
> - `.update(data)` — כמו merge, אבל **נכשל** אם המסמך לא קיים. set(merge) יוצר אם לא קיים.

---

# 📋 חלק 4 — גיליון בגידה של 2 דקות

> קרא את זה ממש לפני שאתה נכנס לחדר.

**מה EasyLex:** אפליקציית Android בJava — צלם מילה → תרגום AI → תרגול → XP

**3 טכנולוגיות ראשיות:**
- Room — DB מקומי של 1,902 מילים + נתוני תרגול
- Firestore — ענן, מילים גלובליות + XP/Streak
- ML Kit + Gemini — OCR + תרגום AI

**Toast / Snackbar / Dialog:**
- Toast = פשוט, נעלם → לדברים קטנים
- Dialog = מחכה לאישור → לפעולות הרסניות

**הבאג הגדול:** סנכרון מחק נתונים → תיקון: Insert-Only + רשימת קיימות + פעם ב-24שעות

**Mastery:** `min(5, floor(נכונות / (ניסיונות+1) × 5))`

**XP:** +10 נכונה | +100 Mastery 5 | +50 סיום מודול

**ViewBinding:** binding.view (בטוח) > findViewById (לא בטוח). binding=null ב-onDestroyView!

**MVVM:** Fragment (מציג) ← ViewModel (לוגיקה, שורד סיבוב) ← Repository ← Room/Firestore

**Singleton:** מופע אחד בלבד. volatile + synchronized = Thread-safe.

**אם לא יודעת תשובה — אמור:**
> "בחרתי X כי Y. החלופה היתה Z, אבל לא בחרתי בה כי [סיבה]."

---

*EasyLex v1.1 | ישיבת הרב עמיאל | 18/6/2026 — בהצלחה! 💪*
