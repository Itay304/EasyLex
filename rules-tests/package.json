{
  "name": "easylex-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore Security Rules unit tests for EasyLex — runs against the local Firebase Emulator only, never against the real database.",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "jest": "^29.7.0"
  }
}
